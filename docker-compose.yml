services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: o_facilitador_frontend
    ports:
      - "3000:80"
    restart: unless-stopped

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: o_facilitador_backend
  #   ports:
  #     - "8080:8080"
  #   restart: unless-stopped
